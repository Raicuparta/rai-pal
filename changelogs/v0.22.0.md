TODO!

- Manual provider cache seems like it's not being cleared properly? Seems to be scanning games that aren't in there anymore.
- Filters are wonky when they change. Like if someone has a saved state from previous filters and the app gets updated with new filters, we start with invalid filter state.
- If the main database fails to load, third party databases won't be attempted at all.
- Changing mod sources makse selected game mod list disappear.
- Toggling mod sources suddenly stopped working?
- Are games refreshing one step behind? Try installing a game, refreshing.

- Support for custom mod sources. This means mod developers can share links for people to easily add third party mods to Rai Pal, using the `rai-pal://` deep link.
- All new Rai Pal releases are now automatically pushed to WinGet. You can install Rai Pal via WinGet with `winget install Raicuparta.RaiPal`.
- Support scanning entire folders for game exes. Can be slow though, so be careful!
- Manually-added games can now run on Linux using Wine if you have that installed in the system or via flatpak.
- Fix native Linux games failing to launch from the "Run Game Executable" button.
- Now detecting GameMaker engine version for installed games.
- Add new filter for finding games compatible with specific mods (like finding all games compatible with UUVR).
- Filers can now be locked, so if you always have certain filters off you can lock them and the reset buttons won't affect them anymore.
- Make it a bit easier to tell manually added games apart by showing folder names when needed.
- Improvements to mod install progress indicators.
- Improved Unreal Engine version detection.
- Shop showing errors when checking for updates fails (seems to fail a lot randomly).
- Generally reduce vague error notifiactions.
- Better handling of a failed mod download and/or install. Will also retry installs in case it's a network blip.
- Retry mod downloads once when they fail.
- Fix mods that run via terminal (like Chihuhahua) being invisible when running on Linux via Wine.
- Fix some Unreal games missing architecture information (64 vs 32 bit).

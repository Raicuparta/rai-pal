Add games manually by browsing for the exe or dropping it on the app window.
Handle sorting for Unity 6+.
Show engine name for installable mods in cases where game engine isn't detected.
Prevent squishing table columns, show horizontal scroll instead.
Allow hiding thumbnails column in filter menu.
Show fallback image for thumbnails (currently for manually added games).

- Show warnings that suggest uninstalling deprecated mods.
- Add button to delete local mods from the Mods tab.
- Add mod settings button in game page (for now this just opens the folder where the settings file are).
- Add mod folder button in game page.
- Add button to run runnable mods (like UEVR) in the Mods tab, so you can easily start UEVR without attaching to any specific game.
- Add button to uninstall all mods for a specific game.
- Mods can now specify which engine version they're compatible with.
- Remove old UEVR score column that was always super outdated and nobody used anyway (maybe we'll come back to this later once we have a proper API).
- Make the filters menu horizontally scrollable if window is tiny.

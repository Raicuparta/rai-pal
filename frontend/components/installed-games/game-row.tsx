// import { Game } from "@api/bindings";
// import { GameExecutableRow } from "./game-executable-row";

// export function GameRow(_: number, game: Game) {
//   return Object.values(game.executables)?.map((executable) => (
//     <tr key={executable.id}>
//       <GameExecutableRow game={game} executable={executable} />
//     </tr>
//   ));
// }
